-- =========================================================
-- 🔥 ABZZAIT STANDALONE KILLALL (TP ALL) SYSTEM 🔥
-- Hỗ trợ phím tắt E để bật/tắt nhanh - Auto Click & NoCollide
-- =========================================================

local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local VirtualUser = game:GetService("VirtualUser")
local lp = Players.LocalPlayer

-- Khởi tạo Anti-AFK
if getconnections then
    for _, connection in pairs(getconnections(lp.Idled)) do
        pcall(function() 
            connection:Disable() 
        end)
    end
end

lp.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new(0, 0))
end)

-- Khởi tạo GUI độc lập
local Gui = Instance.new("ScreenGui", lp:WaitForChild("PlayerGui"))
Gui.Name = "Abzzait_KillAll_System"
Gui.ResetOnSpawn = false

-- Khung giao diện nhỏ gọn (260x220)
local MainFrame = Instance.new("Frame", Gui)
MainFrame.Size = UDim2.new(0, 260, 0, 220)
MainFrame.Position = UDim2.new(0.5, -130, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 6)

-- Hệ thống kéo thả GUI
local drag, start, pos
MainFrame.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        drag = true
        start = i.Position
        pos = MainFrame.Position
    end
end)

UIS.InputChanged:Connect(function(i)
    if drag and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        local delta = i.Position - start
        MainFrame.Position = UDim2.new(pos.X.Scale, pos.X.Offset + delta.X, pos.Y.Scale, pos.Y.Offset + delta.Y)
    end
end)

UIS.InputEnded:Connect(function() 
    drag = false 
end)

-- Tiêu đề bảng điều khiển
local Title = Instance.new("TextLabel", MainFrame)
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1, -30, 0, 30)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.Text = "⚔️ Abzzait KillAll (TP All) System"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 11
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Nút tắt nhanh bảng (X)
local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Size = UDim2.new(0, 20, 0, 20)
CloseBtn.Position = UDim2.new(1, -25, 0, 5)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "X"
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 12
CloseBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
CloseBtn.MouseButton1Click:Connect(function() 
    Gui:Destroy() 
end)

-- Phím tắt bật/tắt hiển thị nhanh bảng điều khiển (RightControl)
UIS.InputBegan:Connect(function(input, processed)
    if not processed then
        if input.KeyCode == Enum.KeyCode.RightControl then
            MainFrame.Visible = not MainFrame.Visible
        end
    end
end)

-- Hàm tạo nút bấm nhanh
local function createButton(parent, text, size, position, callback)
    local btn = Instance.new("TextButton", parent)
    btn.Size = size
    btn.Position = position
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    btn.Text = text
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 10
    btn.TextColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- =========================================================
-- [CORE LOGIC - HỆ THỐNG KILLALL (TP ALL)]
-- =========================================================
local toggledKillAll = false
local loopKillAllActive = false
local BACK_OFFSET_DISTANCE = 2.0 -- Khoảng cách đứng sau mục tiêu
local SPEED_TUNER = 0.02 -- Tốc độ delay giữa mỗi lần dịch chuyển

-- Lấy danh sách tất cả mục tiêu hợp lệ
local function GetTargetsIncludingDummies()
    local targets = {}
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= lp and p.Character then
            local hum = p.Character:FindFirstChild("Humanoid")
            if hum and hum.Health > 0 then
                table.insert(targets, p.Character)
            end
        end
    end
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("Model") then
            local hum = obj:FindFirstChild("Humanoid")
            local hrp = obj:FindFirstChild("HumanoidRootPart")
            if hum and hrp then
                if not Players:GetPlayerFromCharacter(obj) and obj ~= lp.Character and hum.Health > 0 then
                    table.insert(targets, obj)
                end
            end
        end
    end
    return targets
end

-- Vòng lặp chính dịch chuyển liên tục
local function RunKillAllLoop()
    if loopKillAllActive then 
        return 
    end
    loopKillAllActive = true
    task.spawn(function()
        while toggledKillAll do
            local targets = GetTargetsIncludingDummies()
            if #targets > 0 then
                local target = targets[math.random(1, #targets)]
                local myChar = lp.Character
                local hrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                local targetHrp = target:FindFirstChild("HumanoidRootPart")
                if hrp and targetHrp then
                    local backOffset = targetHrp.CFrame.LookVector * BACK_OFFSET_DISTANCE
                    local targetPos = Vector3.new(targetHrp.Position.X - backOffset.X, targetHrp.Position.Y, targetHrp.Position.Z - backOffset.Z)
                    myChar:PivotTo(CFrame.new(targetPos, targetHrp.Position))
                end
            end
            task.wait(SPEED_TUNER)
        end
        loopKillAllActive = false
    end)
end

-- Vòng lặp tự động click tấn công và tắt va chạm
RunService.Heartbeat:Connect(function()
    if toggledKillAll then
        if lp.Character then
            for _, part in pairs(lp.Character:GetChildren()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
        VirtualUser:CaptureController()
        VirtualUser:Button1Down(Vector2.new(0, 0))
    end
end)

-- =========================================================
-- [GIAO DIỆN ĐIỀU KHIỂN CHI TIẾT]
-- =========================================================

local ToggleBtn

-- Hàm xử lý Bật/Tắt KillAll dùng chung cho Click và Phím tắt E
local function ToggleKillAll()
    toggledKillAll = not toggledKillAll
    if toggledKillAll then
        ToggleBtn.Text = "KillAll: ACTIVE (E)"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
        RunKillAllLoop()
    else
        ToggleBtn.Text = "KillAll: OFF (E)"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    end
end

-- 1. Nút Bật / Tắt KillAll chính (Hiện chữ phím tắt E)
ToggleBtn = createButton(MainFrame, "KillAll: OFF (E)", UDim2.new(1, -20, 0, 32), UDim2.new(0, 10, 0, 35), ToggleKillAll)

-- Lắng nghe phím E để kích hoạt bật/tắt nhanh
UIS.InputBegan:Connect(function(input, processed)
    if not processed then
        if input.KeyCode == Enum.KeyCode.E then
            ToggleKillAll()
        end
    end
end)

-- 2. Thanh chỉnh khoảng cách sau lưng (Back Offset)
local OffsetFrame = Instance.new("Frame", MainFrame)
OffsetFrame.Size = UDim2.new(1, -20, 0, 26)
OffsetFrame.Position = UDim2.new(0, 10, 0, 75)
OffsetFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Instance.new("UICorner", OffsetFrame).CornerRadius = UDim.new(0, 4)

local OffsetLabel = Instance.new("TextLabel", OffsetFrame)
OffsetLabel.Size = UDim2.new(1, -80, 1, 0)
OffsetLabel.Position = UDim2.new(0, 8, 0, 0)
OffsetLabel.BackgroundTransparency = 1
OffsetLabel.Text = "Back Distance: " .. BACK_OFFSET_DISTANCE .. " studs"
OffsetLabel.Font = Enum.Font.GothamBold
OffsetLabel.TextSize = 9
OffsetLabel.TextColor3 = Color3.fromRGB(255, 165, 0)
OffsetLabel.TextXAlignment = Enum.TextXAlignment.Left

createButton(OffsetFrame, "-", UDim2.new(0, 24, 0, 18), UDim2.new(1, -58, 0, 4), function()
    if BACK_OFFSET_DISTANCE > 0.5 then
        BACK_OFFSET_DISTANCE = BACK_OFFSET_DISTANCE - 0.5
        OffsetLabel.Text = "Back Distance: " .. BACK_OFFSET_DISTANCE .. " studs"
    end
end)

createButton(OffsetFrame, "+", UDim2.new(0, 24, 0, 18), UDim2.new(1, -28, 0, 4), function()
    if BACK_OFFSET_DISTANCE < 10 then
        BACK_OFFSET_DISTANCE = BACK_OFFSET_DISTANCE + 0.5
        OffsetLabel.Text = "Back Distance: " .. BACK_OFFSET_DISTANCE .. " studs"
    end
end)

-- 3. Thanh chỉnh Delay Speed (Tốc độ quét)
local SpeedFrame = Instance.new("Frame", MainFrame)
SpeedFrame.Size = UDim2.new(1, -20, 0, 26)
SpeedFrame.Position = UDim2.new(0, 10, 0, 109)
SpeedFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Instance.new("UICorner", SpeedFrame).CornerRadius = UDim.new(0, 4)

local SpeedLabel = Instance.new("TextLabel", SpeedFrame)
SpeedLabel.Size = UDim2.new(1, -80, 1, 0)
SpeedLabel.Position = UDim2.new(0, 8, 0, 0)
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Text = "Delay: " .. SPEED_TUNER .. "s"
SpeedLabel.Font = Enum.Font.GothamBold
SpeedLabel.TextSize = 9
SpeedLabel.TextColor3 = Color3.fromRGB(0, 220, 255)
SpeedLabel.TextXAlignment = Enum.TextXAlignment.Left

createButton(SpeedFrame, "-", UDim2.new(0, 24, 0, 18), UDim2.new(1, -58, 0, 4), function()
    if SPEED_TUNER > 0.01 then
        SPEED_TUNER = SPEED_TUNER - 0.01
        SpeedLabel.Text = "Delay: " .. string.format("%.2f", SPEED_TUNER) .. "s"
    end
end)

createButton(SpeedFrame, "+", UDim2.new(0, 24, 0, 18), UDim2.new(1, -28, 0, 4), function()
    if SPEED_TUNER < 0.5 then
        SPEED_TUNER = SPEED_TUNER + 0.01
        SpeedLabel.Text = "Delay: " .. string.format("%.2f", SPEED_TUNER) .. "s"
    end
end)

-- Chân trang hiển thị phím tắt ẩn/hiện bảng điều khiển chính
local HelpLabel = Instance.new("TextLabel", MainFrame)
HelpLabel.Size = UDim2.new(1, -20, 0, 20)
HelpLabel.Position = UDim2.new(0, 10, 1, -25)
HelpLabel.BackgroundTransparency = 1
HelpLabel.Text = "Phím ẩn/hiện bảng: RightControl | Kích hoạt: E"
HelpLabel.Font = Enum.Font.GothamBold
HelpLabel.TextSize = 8
HelpLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
HelpLabel.TextXAlignment = Enum.TextXAlignment.Center

print("⚔️ [Abzzait Standalone KillAll] Đã cấu hình hoàn tất phím tắt E!")
